# Server Configuration
server.port=8080

# Application Name
spring.application.name=EduQuest

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.timeout=60000

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=quanlisvhs@gmail.com
spring.mail.password=kjak ucgm bbpj mgdo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/EduQuest
spring.datasource.username=postgres
spring.datasource.password=1234


# Security configuration (optional)
spring.security.user.name=admin
spring.security.user.password=admin123


# Jwt Configuration
EduQuest.jwt.secret=your_jwt_secret_key
EduQuest.jwt.expiration=3600000
EduQuest.jwt.refresh.expiration=2592000
EduQuest.jwt.audience=EduQuestAudience
EduQuest.jwt.issuer=EduQuestIssuer
EduQuest.jwt.key.refresh-token=Refresh:refreshToken_

# Custom Application Properties
app.otp.cache.prefix=otp:
app.otp.cache.expiry.minutes=5
app.token.blacklisted.prefix=blacklisted_token:
app.otp.verified.prefix=otp_verified:
app.token.expiry.minutes=5
app.otp.verified.expiry.minutes=5
# OTP Configuration
otp.expiration=300
# Redis Key Constants
# eduquest.redis.key.otp-resend-prefix=eduquest:RESEND otp:
eduquest.redis.key.otp-verify-prefix=eduquest:verify otp:
eduquest.redis.key.auth-cache-prefix=eduquest:auth:
eduquest.redis.key.default-otp-expiration=300